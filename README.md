# Sensors Data App

Start the application:

```sh
npm -i
json-server --watch data/db.json
npm start # or "ng serve"
```

Task requirements are in the "requirements/FE Dev Test.odt" file.  
There is `sample_db.json` db mock file as well.
