# Sensors Data App

Start the application:

```sh
npm -i
json-server --watch data/db.json
npm start # or "ng serve"
```

Task requirements:

Task requirements are in the `requirements/FE Dev Test.odt` file as well as `sample_db.json` db mock file.
